/**
 * Timing constants for consistent delays and intervals across the application
 */

// Navigation delays
export const SIDEBAR_CLOSE_DELAY_MS = 100 // Just enough for sidebar to start closing
export const NAVIGATION_DELAY_MS = 400 // Reduced from 700ms for better performance

// Cache revalidation times
export const REVALIDATION_TIME_SECONDS = 300 // 5 minutes ISR revalidation

// Animation timings
export const ANIMATION_DURATION_MS = 300
export const QUICK_ANIMATION_MS = 150
